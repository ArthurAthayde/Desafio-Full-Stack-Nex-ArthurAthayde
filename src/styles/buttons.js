import { styled } from "styled-components";

export const StyledRegisterBtn = styled.button`
  display: inline-flex;
  align-items: center;
  justify-content: center;

  min-height: 48px;
  width: 100%;

  border: solid 1px var(--grey-3);
  border-radius: 4px;

  padding: 0px 22px;

  font-size: 1rem;
  font-weight: 500;
  color: var(--grey-3);

  background-color: var(--color-emphasis-1);
`;
